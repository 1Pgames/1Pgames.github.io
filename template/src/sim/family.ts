/**
 * Default family for `npm run sim` — rewritten by `scripts/new-game.sh
 * --family <code>` at scaffold time so a game's sim gates match its slice.
 * The template ships the arena reference slice.
 */
export const SIM_FAMILY = 'arena';
